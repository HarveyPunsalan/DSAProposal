And for this in my Milestone 2. Even though I haven't opened the module yet, I can see its title. And I just watched the recordings of the sync session again and saw the topics that should be included in Milestone 2. Here are my recommendations.
1. add new stock entries - use hash table and the algorithm is hashing with collision handling
2. delete stock entries - use binary search tree and the algorithm is node deletion
3. sorting stocks according to brand - use array and the algorithm is merge sort
4. searching for an existing inventory using any available criteria - use binary search tree and the algorithm is BST search.
